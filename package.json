{
  "name": "dsh-team-auto-router",
  "description": "Complexity-triggered auto activation for dsh-agent-teams: scores each genuine user message at the agent/pre-step boundary and, past the configured threshold, injects an ask-for-confirmation or a deterministic AgentTeams activation directive. Fully inert by default (autoTrigger: off).",
  "version": "0.1.2",
  "type": "module",
  "main": "lib/index.js",
  "exports": {
    ".": "./lib/index.js",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "prepublishOnly": "npm test"
  },
  "dependencies": {
    "@deepseek-ai/schemastery": "^3.18.1",
    "@deepseek-ai/dsh-llm": "^0.1.1-rc.2"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/snow930/dsh-team-auto-router.git"
  },
  "files": [
    "lib",
    "cordis.patch.yml",
    "README.md"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  }
}
